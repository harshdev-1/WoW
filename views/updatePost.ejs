<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Post</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }

        form {
            max-width: 600px;
            margin: auto;
        }

        label {
            display: block;
            margin-bottom: 8px;
        }

        input, textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            box-sizing: border-box;
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>

    <h2>Edit Post</h2>

    <form action="/user/update/post/<%= PostId %>" method="post">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" placeholder="<%= tiltle.postTitle %>" required>

        <label for="content">Content:</label>
        <textarea id="content" name="content" placeholder="<%= tiltle.postText %>" aria-valuenow="<%= tiltle.postText %>" rows="8" required></textarea>

        <button type="submit">Update Post</button>
    </form>
    <script>
        const tiltle = document.querySelector('#title')
        const text = document.querySelector('#content')
        function hak(){
            tiltle.value = `<%= tiltle.postTitle %>`
            text.value = `<%= tiltle.postText %>`
        }
        hak()
    </script>
</body>
</html>
